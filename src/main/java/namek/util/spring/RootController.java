package namek.util.spring;

//@RestController
public class RootController {
  //
  // @GetMapping
  // RepresentationModel index() {
  // RepresentationModel rootResource = new RepresentationModel();
  //
  // rootResource.add(linkTo(methodOn(BackofficePlayerController.class).all("")).withRel("players"));
  //
  // rootResource.add(linkTo(methodOn(BackofficeLogActivityController.class).all(PageRequest.of(1, 10000),
  // "")).withRel("logActivities"));
  //
  // rootResource
  // .add(linkTo(methodOn(BackofficeAccountHistoryController.class).all(PageRequest.of(1, 10000),
  // "")).withRel("accountHistories"));
  //
  // rootResource.add(linkTo(methodOn(BackofficePaymentController.class).all(PageRequest.of(1, 10000),
  // "")).withRel("payments"));
  //
  // rootResource.add(linkTo(methodOn(BackofficeBonusNotActivatedController.class).all()).withRel("bonusesNotActivated"));
  //
  // return rootResource;
  // }
  //
}
